import { CommandLineUI } from "../cli/CommandLine.js";

export default async function () {
    /* Function to provide the user access to the ig.bot interface */
    console.log(`Boot up the ig.bot`);
}
