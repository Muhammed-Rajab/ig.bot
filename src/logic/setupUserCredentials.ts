import { CommandLineUI } from "../cli/CommandLine.js";

export default async function () {
    /* A function to setup and save user's instagram credentials */
    console.log(`Setup the user credentials`);
}
