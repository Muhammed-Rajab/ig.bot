import { CommandLineUI } from "../cli/CommandLine.js";

export default async function () {
    /* Function to manually check if the username and password are correct*/
    console.log(`Validate the user credentials`);
}
